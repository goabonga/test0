apiVersion: v1
kind: Service
metadata:
  name: {{ include "shomer.fullname" . }}
  labels:
    {{- include "shomer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "shomer.selectorLabels" . | nindent 4 }}
